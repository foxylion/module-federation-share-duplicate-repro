{
  "name": "module-federation-share-duplicate-repro",
  "private": true,
  "scripts": {
    "dev": "pnpm run -r --workspace-concurrency=Infinity \"/^dev:.*/\"",
    "preview": "pnpm run -r --workspace-concurrency=Infinity \"/^preview:.*/\""
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "prettier": "^3.5.3"
  }
}
