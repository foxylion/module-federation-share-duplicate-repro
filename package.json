{
  "name": "module-federation-share-duplicate-repro",
  "private": true,
  "scripts": {
    "dev:host": "pnpm run -r dev",
    "dev:remotes": "pnpm run -r --workspace-concurrency=Infinity \"/^dev:.*/\""
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "prettier": "^3.5.3"
  }
}
